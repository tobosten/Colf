git add .
git commit -m
git push




New Branch: git checkout <branch>
When change done: git checkout master
Merges when in master: git merge <branch>
Removes selected branch: git branch -d <branch>


9-hole calculation:

(Strokes in 9 holes x 113/Slope Rating + second nine par + 0.5 x Course Handicap) – Course Rating.
second nine counts at par for first nine.

Ex.
(30 * 113 / 94) + 30 + (0.5 * 32) - 57.8
